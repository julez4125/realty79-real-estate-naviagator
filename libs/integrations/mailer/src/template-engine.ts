/**
 * Renders a named email template with the given variable substitutions.
 * Templates are resolved from a conventional templates/ directory.
 *
 * TODO M5.2: implement — load template file, apply Handlebars (or similar)
 * variable interpolation, return rendered string. Add template caching.
 */
export function renderTemplate(
  _name: string,
  _vars: Record<string, unknown>
): string {
  throw new Error('renderTemplate not yet implemented — see M5.2');
}
